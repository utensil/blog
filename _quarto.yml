project:
  type: hugo
  # output-dir: ../dummy
      
format: 
  hugo-md:
    code-fold: true
  
execute:
  warning: false
  # Reuse committed _freeze/ instead of re-executing (e.g. Julia) on every render.
  # Freeze is refreshed via scripts/freeze.sh; tangled renders Julia-free off it.
  freeze: auto

# # supppress HTML output
# to: markdown

# biliography: references.lib