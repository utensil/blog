---
title: Hello, Typst
date: "2024-04-19"
draft: true
# publishDate: 2025-01-01T00:00:00+08:00
format:
  hugo-md: default
  typst:
    toc: true
    toc-depth: 2
    toc-title: Contents
    papersize: a5
    margin:
      x: 1cm
      y: 1cm
    section-numbering: 1.1.a
    columns: 1
# keep-md: true
# keep-typ: true
# prefer-html: true
# https://stackoverflow.com/questions/68036749/embedding-iframe-in-hugo-site
# iframe: "./index.pdf"
---

The following Typst code are taken from [the official example](https://github.com/typst/typst?tab=readme-ov-file#example) for now.

```{=html}
<iframe style="width: 100%; min-height: 800px" src="./index.pdf"></iframe>
```

```{=typst}
// #set page("us-letter")
// #set heading(numbering: "1.")

= Fibonacci sequence

The Fibonacci sequence is defined through the
recurrence relation $F_n = F_(n-1) + F_(n-2)$.
It can also be expressed in _closed form:_

$ F_n = round(1 / sqrt(5) phi.alt^n), quad
  phi.alt = (1 + sqrt(5)) / 2 $

#let count = 8
#let nums = range(1, count + 1)
#let fib(n) = (
  if n <= 2 { 1 }
  else { fib(n - 1) + fib(n - 2) }
)

The first #count numbers of the sequence are:

#align(center, table(
  columns: count,
  ..nums.map(n => $F_#n$),
  ..nums.map(n => str(fib(n))),
))
```
